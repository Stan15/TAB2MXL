package converter;

public class GuitarNote extends Note{
    public GuitarNote(int stringNumber, int fret, int duration, int distanceToMeasureStart) {
        super(stringNumber, fret, duration, distanceToMeasureStart);
    }
}
