package converter.converter_TryVersion;
//Never mind it, yet.
//It's try version to test converter of mine(boho), because I don't want to ruin the current version that we've made.
//If it works well, we can use it later.

public interface ScriptComponents {
    String basicInfo();
    String defaultInfo();
    String partlistInfo();
    String partInfo();
    String mergeAllscripts();
}
